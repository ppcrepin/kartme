# Clés publiques Supabase pour l'app (client).
# En local : copie ce fichier en `.env` et remplis les valeurs.
# En prod web : injectées par la CI depuis les secrets GitHub (SUPABASE_URL / SUPABASE_ANON_KEY).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
