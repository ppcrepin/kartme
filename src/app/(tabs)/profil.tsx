import { Screen, Muted } from '@/components/screen';

export default function ProfilScreen() {
  return (
    <Screen title="Profil">
      <Muted>Ton Elo, ton grade et tes badges — à venir.</Muted>
    </Screen>
  );
}
