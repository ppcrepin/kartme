import { Screen, Muted } from '@/components/screen';

export default function AmisScreen() {
  return (
    <Screen title="Amis">
      <Muted>Demandes et profils — à venir.</Muted>
    </Screen>
  );
}
